# stdbool Compatibility Slot

Reserved for a portable `<stdbool.h>` shim when targeting platforms that lack
the standard header. Currently unused; drop the shim here if you encounter such
a platform.
