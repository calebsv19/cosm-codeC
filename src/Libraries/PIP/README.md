# PIP Vendor Slot

Reserved for Python packages installed via `pip` when embedding Python inside
the IDE. Currently empty; add modules here alongside a short description when
Python integration begins.
